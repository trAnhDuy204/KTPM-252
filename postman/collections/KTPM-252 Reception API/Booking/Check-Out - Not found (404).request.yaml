$kind: http-request
url: "{{baseUrl}}/api/reception/bookings/99/check-out"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('Status 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 12000
