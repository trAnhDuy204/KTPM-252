$kind: http-request
url: "{{baseUrl}}/api/reception/bookings/1"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Has booking id', () => pm.expect(pm.response.json().id).to.be.a('number'));
    language: text/javascript
order: 17000
