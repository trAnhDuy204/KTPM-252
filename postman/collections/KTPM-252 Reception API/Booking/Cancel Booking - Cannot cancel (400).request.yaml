$kind: http-request
url: "{{baseUrl}}/api/reception/bookings/1/cancel"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('Status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 14000
