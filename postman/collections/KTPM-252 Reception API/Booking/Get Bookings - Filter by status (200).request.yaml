$kind: http-request
url: "{{baseUrl}}/api/reception/bookings?status=CHECKED_IN"
method: GET
queryParams:
  status: CHECKED_IN
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 16000
