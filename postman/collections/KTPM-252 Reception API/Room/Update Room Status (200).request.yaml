$kind: http-request
url: "{{baseUrl}}/api/reception/rooms/10/status"
method: PATCH
body:
  type: json
  content: |-
    {
        "status": "OCCUPIED"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Status updated', () => pm.expect(pm.response.json().status).to.eql('OCCUPIED'));
    language: text/javascript
order: 14000
