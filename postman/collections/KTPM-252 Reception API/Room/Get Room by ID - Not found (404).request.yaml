$kind: http-request
url: "{{baseUrl}}/api/reception/rooms/99"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 404', () => pm.response.to.have.status(404));
      pm.test('Error message', () => pm.expect(pm.response.json().message).to.eql('Room not found with id: 99'));
    language: text/javascript
order: 13000
