$kind: http-request
url: "{{baseUrl}}/api/reception/rooms?hotelId=99"
method: GET
queryParams:
  hotelId: "99"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Empty array', () => pm.expect(pm.response.json()).to.have.lengthOf(0));
    language: text/javascript
order: 11000
