$kind: http-request
url: "{{baseUrl}}/api/reception/rooms/10/status"
method: PATCH
body:
  type: json
  content: "{}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 400', () => pm.response.to.have.status(400));
      pm.test('Validation error', () => pm.expect(pm.response.json().validationErrors.status).to.eql('status is required'));
    language: text/javascript
order: 15000
