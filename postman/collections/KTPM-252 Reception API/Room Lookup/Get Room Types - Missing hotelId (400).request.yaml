$kind: http-request
url: "{{baseUrl}}/api/reception/room-types"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 4000
