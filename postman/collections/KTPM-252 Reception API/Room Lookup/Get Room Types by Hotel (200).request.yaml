$kind: http-request
url: "{{baseUrl}}/api/reception/room-types?hotelId=1"
method: GET
queryParams:
  hotelId: "1"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Is array', () => pm.expect(pm.response.json()).to.be.an('array'));
    language: text/javascript
order: 3000
