# Use this workspace to collaborate
workspace:
  id: ""

localResources:
  collections:
    - ..\backend\Hotel_Management_API.postman_collection.json

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ..\postman\collections\KTPM-252 Reception API: ktpm-252-reception
